name: ConnPerm
description: An application demonstrating setting Location Permissions for the Connectivity Package

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  connectivity: ^0.4.8+6
  cupertino_icons: ^0.1.3
  permission_handler: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
